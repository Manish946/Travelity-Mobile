<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Travelity.Templates.TemplateViews.ChatDataTemplate.OutgoingViewCell">
    <Grid BackgroundColor="#10152A">
        <StackLayout Spacing="0"
                     Padding="0"
                     Margin="0">

            <Frame BackgroundColor="{StaticResource Primary}"
                   Padding="13"
                   CornerRadius="30"
                   FlowDirection="LeftToRight"
                   HasShadow="false"
                   Margin="80,10,10,10">
                <Grid>
                    <StackLayout>
                        <Label FontSize="15"
                               Text="{Binding Text}" />

                    </StackLayout>

                </Grid>

            </Frame>
            <Label FlowDirection="LeftToRight"
                   Padding="10,0,0,0"
                   Margin="80,0,0,0"
                   FontSize="8"
                   TextColor="LightBlue"
                   Text="{Binding dateTime,StringFormat='{0:hh\\:mm}'}" />
        </StackLayout>

    </Grid>
</ViewCell>